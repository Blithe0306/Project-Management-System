<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="usersource_info">
	  <select id="queryUS" parameterMap="parameterMap_us"
		resultClass="usInfo">
		<![CDATA[
		select * from ( 
		    select rownum as rn, t.* from ( 
		    	select id,
				   sourcename,
				   sourcetype,
				   dbtype,
				   serveraddr,
				   port,
				   username,
				   pwd,
	    		   dbname,
	    		   dbtablename,
	    		   filter,
	    		   basedn,
	    		   dominodriver,
	    		   namesfile,
	    		   timeout,
	    		   descp 
    		   from otppms_usersource where 1=1 
         ]]>
           <dynamic prepend="">
			<isNotNull property="sourcename" prepend="and">
				<isNotEmpty property="sourcename">
					sourcename like '%$sourcename$%'
				</isNotEmpty>
			</isNotNull>
		</dynamic>
		<![CDATA[
		 order by sourcename asc) t where rownum < = #endRow#) tt where rn > #startRow# order by sourcename asc 
        ]]>
	</select>
 
</sqlMap>
