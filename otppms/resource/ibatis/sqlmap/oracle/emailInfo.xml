<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="email_info">
	<select id="selectEI" parameterMap="parameterMap_email"
		resultClass="emailInfo">
		<![CDATA[
		select * from (
		    select rownum as rn,t.* from ( 
		    	select id,
		    	account,
		    	pwd,
		    	sender,
		    	servname,
		    	host,
		    	port,
		    	validated,
		    	isdefault,
		    	descp 
	    	FROM otppms_emailinfo 
	    	WHERE 1=1 
        ]]>
		<include refid="queryFilter" />
		<![CDATA[
             order by id desc) t where rownum < = #endRow#) tt where rn > #startRow# order by id desc 
		]]>
	</select>
	
 
</sqlMap>
